
  name: checkerboard
  version: '0.1'
  summary: checkerboard
  description: |
    Checkerboard is C program to generate checker image file with pmg format.
    Example to run, $ ./checkerboard checker.pgm

  grade: devel
  confinement: devmode
  
  parts:
    checkerboard:
      plugin: autotools
      source-type: zip
      source: https://github.com/h1fujiwara/checkerboard/archive/master.zip
      build-packages:
      - build-essential      
  
